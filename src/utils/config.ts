export const AI_CONFIG = {
  THRESHOLDS: {
    COSINE: 0.75,
    LIVENESS: 0.80,
  },
  MODEL_SIZES: {
    MAX_TOTAL_MB: 20,
  },
};

export const SYNC_CONFIG = {
  BATCH_SIZE: 50,
  MAX_RETRY_ATTEMPTS: 5,
};